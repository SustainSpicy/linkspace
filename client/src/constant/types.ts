export interface alertProps {
  show?: boolean;
  text: string;
  type: string;
}

export interface User {
  email: string;
  firstName: string;
  lastName: string;
}

export interface Profile {
  email: string;
  firstName: string;
  lastName: string;
}
